import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  headerContent: {
    flexDirection: 'row',
    alignItems: 'center',
    paddingTop: 10,
    paddingBottom: 10,
  },
  headerLogo: {
    width: 64,
    height: 64,
    resizeMode: 'contain'
  }
});